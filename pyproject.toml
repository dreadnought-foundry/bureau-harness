[project]
name = "harness-pkg"
version = "0.0.0"
requires-python = ">=3.12"
[project.optional-dependencies]
dev = [
  "pytest==7.0.0",   # deliberately OLD — Dependabot keeps filing real PRs
  "pyyaml",          # tests/test_harness_proves_main.py parses ci.yml (DRE-4280); unpinned so it is not a second fixture
]
