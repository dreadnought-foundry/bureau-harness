# bureau-harness
Sandbox repo for the pipeline integration harness (DRE-2073). Do not build product
work here — the bureau-pipeline harness driver dispatches synthetic end-to-end
scenarios against this repo to prove the pipeline before a release.
